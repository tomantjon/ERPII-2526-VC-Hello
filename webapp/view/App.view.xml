<mvc:View controllerName="be.hogent.hellofiorivc.controller.App"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns="sap.m">
    <App id="App">
        <Page id="page"
            title="{model:'i18n', path:'title'}">
            <Input id="txtName"
                placeholder="Enter name"
                valueStateText="Name must not be empty. Maximum 10 characters."
                value="{
                model : 'custom',
				path : '/name',
				type : 'sap.ui.model.type.String',
				constraints : {
					minLength: 1,
					maxLength: 10
				}
			}"
                change= ".onNameChange" />
            <Button id="btnHello"
                type="Default"
                text="{custom>/buttonText}"
                press="onPress"></Button>
        </Page>
    </App>
</mvc:View>